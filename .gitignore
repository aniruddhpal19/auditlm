# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Environment / secrets
.env
*.key
.claude/

# Local planning docs — not tracked
CLAUDE.md
*_spec.md

# Model weights & fine-tuning artifacts (regenerable; release via HF Hub, not git)
*.gguf
*.safetensors
*.bin
*.pt
training/adapter*/
training/fused/
training/data/mlx_*/

# Build artifacts / caches (rebuild from source)
verify/data/identifier_index.json
*.faiss
*.index
embeddings_cache/
data/raw/
data/cache/
data/raw_html/

# Derived corpus outputs (regenerable from the per-source collector files via the pipeline)
data/corpus/unified.jsonl
data/corpus/chunks.jsonl

# Eval run outputs (regenerable; scorecards live in assurancebench/results/)
runs/
demo/build.log

# OS / editor
.DS_Store
.vscode/
.idea/

# Local planning docs
*_PLAN.md

# Local build/debug files
dense_build_error.txt
*.py.bak
